// Development runtime config
// Used during local development (npm run dev)
// In production, generated dynamically by docker-entrypoint.sh
window.APP_CONFIG = {
  API_URL: "http://localhost:8083",
  ENV: "development",
  VERSION: "dev"
};
